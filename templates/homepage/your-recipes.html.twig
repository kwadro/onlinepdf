{% set setting = site_settings() %}
{% if isLogin %}

{% else %}
    <div class="d-flex background-secondary">88888888888</div>
<div class="d-block mt-4" >
    <h5 class="text-center">{{ 'To start creating a recipe collection, please'|trans }}</h5>
    <h5 class="text-center">
        <a class="link" href="{{ path('app_register') }}">{{ 'registered'|trans }}</a>
        {{ 'or'|trans }}
        <a class="link" href="{{ path('app_login','locale'=>setting.locale) }}">{{ 'sign in'|trans }}.</a>
    </h5>
</div>
{% endif %}
