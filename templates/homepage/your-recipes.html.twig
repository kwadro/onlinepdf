{% if isLogin %}
    <div class="d-flex background-secondary">your-recipes.html.twig</div>
{% else %}
<div class="d-block mt-4" >
    <h5 class="text-center">{{ 'To start creating a recipe collection, please'|trans }}</h5>
    <h5 class="text-center">
        <a class="link" href="{{ path('app_register') }}">{{ 'зареєструтесь'|trans }}</a>
        {{ 'або'|trans }}
        <a class="link" href="{{ path('app_login') }}">{{ 'увійдіть'|trans }}.</a>
    </h5>
</div>
{% endif %}
