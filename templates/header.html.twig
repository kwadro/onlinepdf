<div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="{{ path('homepage') }}" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
            <img src="{{ asset('images/logo.png') }}" alt="" width="24" height="24" style="vertical-align:middle; margin-right:8px;">
            <span class="fs-4">{{ 'PDF Editor'|trans }}</span>
        </a>

        <ul class="nav nav-pills">
            <li class="nav-item"><a href="{{ path('homepage') }}" class="nav-link {% if 'homepage' in app.request.get('_route')  %}active {% endif %}" aria-current="page">Home</a></li>
            <li class="nav-item"><a href="{{ path('about') }}" class="nav-link {% if 'about' in app.request.get('_route')  %}active {% endif %}">{{'About'|trans}}</a></li>
            <li class="nav-item"><a href="{{ path('contact') }}" class="nav-link {% if 'contact' in app.request.get('_route')  %}active {% endif %}">{{'Contact'|trans}}</a></li>
{#            <li class="nav-item"><a href="#" class="nav-link">Pricing</a></li>#}
{#            <li class="nav-item"><a href="#" class="nav-link">FAQs</a></li>#}

            <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="dropdown-language" role="button"
                                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.locale|upper }}
                        </a>
                    <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown-language">
                            <li>
                                <a class="dropdown-item" href="{{ path('switch_language', {_locale: 'en'}) }}">{{'English'|trans}}</a></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('switch_language', {_locale: 'ua'}) }}">{{'Ukrainian'|trans}}</a>
                            </li>
                    </ul>
                </li>
        </ul>
    </header>
</div>
