{# templates/admin/import/product.html.twig #}
{% extends '@EasyAdmin/layout.html.twig' %}


{% block content %}
    <div class="container py-4">
        <h1 class="mb-4">Yaml Import {{ entity|split('\\')|last }}</h1>

        {# Flash messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endfor %}

        <div class="card shadow-sm p-4">
            <form method="post" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="file" class="form-label">Choose YAML file</label>
                    <input type="file" class="form-control" id="file" name="file" accept=".yaml" required>
                </div>
                <button type="submit" class="btn btn-primary">Import YAML</button>
            </form>
        </div>
    </div>
{% endblock %}


