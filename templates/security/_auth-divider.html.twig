<div class="auth-divider" role="separator">
    <span class="auth-divider__line"></span>
    {% if label is defined and label %}
        <span class="auth-divider__label">{{ label|trans }}</span>
    {% endif %}
    <span class="auth-divider__line"></span>
</div>
