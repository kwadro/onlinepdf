{%  set content = content|default('OR') %}
<div class="d-flex align-items-center my-2">
    <div class="flex-grow-1 border-top"></div>
    <span class="px-3 text-muted fw-semibold">{{ content|trans }}</span>
    <div class="flex-grow-1 border-top"></div>
</div>
