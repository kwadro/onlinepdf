{%- for group_name, group in groups.items() %}
    {%- for field_name, field in group.entities.items() %}
        {%- if not field.disable_admin %}
use App\Entity\{{ field_name }};
        {%- endif %}
    {%- endfor %}
{%- endfor %}
